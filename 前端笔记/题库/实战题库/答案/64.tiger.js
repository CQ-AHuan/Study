// 实现lodash的findKey方法
// https://www.lodashjs.com/docs/lodash.findKey

function findKey(obj, props) {
    // to do
    return xxx;
}

const o = { a: 1, b: '2', c: 3 };
console.log( findKey(o, ['a', 'c']) ); // { 'b': '2' }
