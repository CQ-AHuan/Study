// 实现lodash的omit方法
// https://www.lodashjs.com/docs/lodash.omit

function omit(obj, props) {
    // to do
    return xxx;
}

const o = { a: 1, b: '2', c: 3 };
console.log( omit(o, ['a', 'c']) ); // { 'b': '2' }
