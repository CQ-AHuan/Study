// 检查数据中是否每一项的value都为true
const input = {
    value: true,
    next: {
        value: true,
        next: {
            value: true,
            next: {
                value: false,
            },
        },
    },
};

function check(input) {
    // to do
    return xxx;
}

console.log(check(input)); // false
